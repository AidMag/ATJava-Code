Part A:
1) roll()
    - uses Math.random() to generate a number between 1-6
2) look()
    - prints the number generated by roll() using ASCII images
3) pair()
    - this method rolls twice then prints all of the dies

Part B:
private int value = the number rolled;

